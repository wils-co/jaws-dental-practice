# Grok Build — Smiles by Design

Own direction, not a fork of agy or Claude Code.

Starts pale (bunq's first stop): a veneer on limestone, then the field
dissolves and the ground inverts to night. Type stays crawlable HTML.

Seat chip on the page: **Grok Build**.

```
python3 -m http.server 8897
# http://127.0.0.1:8897/
python3 prototypes/publish.py
```
